services:
  traefik:
    image: traefik:v3.6
    container_name: traefik
    restart: unless-stopped
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
    networks:
      - proxy_network
networks:
  proxy_network:
    name: your-network
    external: true
